[Unit]
Description=Genres rw neo4j Sidekick
BindsTo=genres-rw-neo4j@%i.service
After=genres-rw-neo4j@%i.service

[Service]
ExecStart=/bin/sh -c "\
  etcdctl set   /ft/services/genres-rw-neo4j/healthcheck true; \
  etcdctl mkdir /ft/services/genres-rw-neo4j/servers; \
  etcdctl set /ft/healthcheck/genres-rw-neo4j-%i/path /__health; \
  while true; do \
    PORT=`[ $PORT ] && echo $PORT || echo $(/usr/bin/docker port genres-rw-neo4j-%i 8080 | cut -d':' -f2)`; \
    etcdctl set /ft/services/genres-rw-neo4j/servers/%i "http://$HOSTNAME:$PORT" --ttl 600; \
    sleep 120; \
  done"

ExecStop=/usr/bin/etcdctl rm /ft/services/genres-rw-neo4j/servers/%i
Restart=on-failure
RestartSec=60

[X-Fleet]
MachineOf=genres-rw-neo4j@%i.service


