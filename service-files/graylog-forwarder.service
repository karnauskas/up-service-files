[Unit]
Description=Graylog forwarder

[Service]
ExecStart=/bin/bash -c "\ 
  export SPLUNK_IP=$(/usr/bin/etcdctl get /ft/splunk_host); \
  /usr/bin/journalctl -a -f --output=json | ncat $SPLUNK_IP 504"
Restart=on-failure
RestartSec=60

[Install]
WantedBy=multi-user.target

[X-Fleet]
Global=true
