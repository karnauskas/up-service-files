[Unit]
Description=Kibana
After=docker.service kafka.service
Requires=docker.service
Wants=elasticsearch-sidekick@%i.service

[Service]
TimeoutStartSec=0
# Change killmode from "control-group" to "none" to let Docker remove
# work correctly.
KillMode=none
ExecStartPre=-/bin/bash -c '/usr/bin/docker kill %p-%i > /dev/null 2>&1'
ExecStartPre=-/bin/bash -c '/usr/bin/docker rm %p-%i > /dev/null 2>&1'
ExecStartPre=/usr/bin/docker pull kibana

ExecStart=/bin/sh -c '\
    export ES_HOST=$(/usr/bin/etcdctl get /ft/config/elasticsearch/1/host); \
    export ES_PORT=$(/usr/bin/etcdctl get /ft/config/elasticsearch/1/port); \
    /usr/bin/docker run --rm --name %p-%i -P --env "ELASTICSEARCH_URL=http://$ES_HOST:$ES_PORT" kibana \
'
ExecStop=/usr/bin/docker stop -t 3 %p-%i
Restart=on-failure
RestartSec=60

[X-Fleet]
Conflicts=%p@*.service
