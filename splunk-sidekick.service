[Unit]
Description=Splunk Sidekick
BindsTo=splunk.service
After=splunk.service

[Service]
ExecStart=/bin/sh -c "\
  while true; do \
    etcdctl set /ft/splunk_host $HOSTNAME --ttl 60; \
    sleep 45; \
  done"
ExecStop=/usr/bin/etcdctl rm /ft/splunk_host

[X-Fleet]
MachineOf=splunk.service
